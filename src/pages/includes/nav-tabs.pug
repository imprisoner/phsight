nav.tabs
    .swiper-container 
        .swiper-wrapper
            block tabs
                mixin tabs(links)
                    each link in links
                        a(href="#").tab-link.swiper-slide= link
                    else
                        p NO LINKS
